"use client";

import { useState } from "react";
import { Dialog as DialogPrimitive } from "radix-ui";
import { Loader2, Save, X } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import FollowUpAutocomplete from "@/components/follow-ups/FollowUpAutocomplete";
import { useUpdateFollowUp } from "@/hooks/use-follow-ups";
import type { FollowUpItem, FollowUpOption, FollowUpType } from "@/types/follow-up";

export default function FollowUpEditDialog({ followUp, open, onOpenChange }: { followUp: FollowUpItem; open: boolean; onOpenChange: (open: boolean) => void }) {
  const [business, setBusiness] = useState<FollowUpOption>({ id: followUp.businessId, label: followUp.businessName, subtitle: followUp.businessCity });
  const [assignee, setAssignee] = useState<FollowUpOption | undefined>(followUp.assignedToId ? { id: followUp.assignedToId, label: followUp.assignedToName } : undefined);
  const [scheduledAt, setScheduledAt] = useState(followUp.scheduledAt ? new Date(followUp.scheduledAt).toISOString().slice(0, 16) : "");
  const [type, setType] = useState<FollowUpType>((followUp.type as FollowUpType) || "CALL");
  const [status, setStatus] = useState(followUp.status);
  const [notes, setNotes] = useState(followUp.notes ?? "");
  const mutation = useUpdateFollowUp();
  const submit = async (event: React.FormEvent) => { event.preventDefault(); if (!business.id || !assignee?.id || !scheduledAt) { toast.error("Business, assignee, and scheduled date are required."); return; } try { await mutation.mutateAsync({ businessId: business.id, assignedTo: assignee.id, type, scheduledAt: new Date(scheduledAt).toISOString(), status, notes }); toast.success("Follow-up updated"); onOpenChange(false); } catch { toast.error("Unable to update follow-up."); } };
  return <DialogPrimitive.Root open={open} onOpenChange={onOpenChange}><DialogPrimitive.Portal><DialogPrimitive.Overlay className="fixed inset-0 z-50 bg-black/40 backdrop-blur-[2px]" /><DialogPrimitive.Content className="fixed left-1/2 top-1/2 z-50 max-h-[90vh] w-[calc(100%-2rem)] max-w-2xl -translate-x-1/2 -translate-y-1/2 overflow-y-auto rounded-2xl border border-[#e4ecf2] bg-white p-6 shadow-2xl outline-none"><div className="flex items-start justify-between border-b border-[#eef2f6] pb-4"><div><DialogPrimitive.Title className="text-lg font-bold text-[#0f172a]">Edit follow-up</DialogPrimitive.Title><DialogPrimitive.Description className="mt-1 text-xs text-[#64748b]">Reschedule, reassign, or update the notes.</DialogPrimitive.Description></div><DialogPrimitive.Close asChild><button type="button" aria-label="Close" className="rounded-lg p-1.5 text-[#94a3b8] hover:bg-[#f1f5f9]"><X className="size-4" /></button></DialogPrimitive.Close></div><form onSubmit={submit} className="mt-5 space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1.5 text-xs font-semibold text-[#475569]">Business<FollowUpAutocomplete kind="business" value={business.id} label={business.label} onChange={(option) => option && setBusiness(option)} placeholder="Select business" /></label><label className="space-y-1.5 text-xs font-semibold text-[#475569]">Assigned to<FollowUpAutocomplete kind="user" value={assignee?.id} label={assignee?.label} onChange={setAssignee} placeholder="Select team member" /></label><label className="space-y-1.5 text-xs font-semibold text-[#475569]">Scheduled date<Input required type="datetime-local" value={scheduledAt} onChange={(event) => setScheduledAt(event.target.value)} className="mt-1 h-10 text-xs" /></label><label className="space-y-1.5 text-xs font-semibold text-[#475569]">Type<Select value={type} onValueChange={(value) => setType(value as FollowUpType)}><SelectTrigger className="mt-1 h-10 text-xs"><SelectValue /></SelectTrigger><SelectContent className="bg-white"><SelectItem value="CALL">Call</SelectItem><SelectItem value="MEETING">Meeting</SelectItem><SelectItem value="EMAIL">Email</SelectItem><SelectItem value="WHATSAPP">WhatsApp</SelectItem></SelectContent></Select></label><label className="space-y-1.5 text-xs font-semibold text-[#475569]">Status<Select value={status} onValueChange={setStatus}><SelectTrigger className="mt-1 h-10 text-xs"><SelectValue /></SelectTrigger><SelectContent className="bg-white"><SelectItem value="SCHEDULED">Scheduled</SelectItem><SelectItem value="COMPLETED">Completed</SelectItem><SelectItem value="CANCELLED">Cancelled</SelectItem></SelectContent></Select></label></div><label className="block space-y-1.5 text-xs font-semibold text-[#475569]">Notes<Textarea value={notes} onChange={(event) => setNotes(event.target.value)} placeholder="Add notes about this follow-up..." className="min-h-28 text-xs" /></label><div className="flex justify-end gap-2 border-t border-[#eef2f6] pt-4"><DialogPrimitive.Close asChild><Button type="button" variant="outline">Cancel</Button></DialogPrimitive.Close><Button type="submit" disabled={mutation.isPending}>{mutation.isPending ? <Loader2 className="size-4 animate-spin" /> : <Save className="size-4" />} Save changes</Button></div></form></DialogPrimitive.Content></DialogPrimitive.Portal></DialogPrimitive.Root>;
}
